% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/Cal_functions.R
\name{cal_ari_nmi}
\alias{cal_ari_nmi}
\title{Calculate Adjusted Rand Index (ARI) and Normalized Mutual Information (NMI)}
\usage{
cal_ari_nmi(lowDim_data, k, method_name, seed, info = NULL)
}
\arguments{
\item{lowDim_data}{A matrix or data frame of low-dimensional data.}

\item{k}{The number of clusters for k-means clustering.}

\item{method_name}{A string naming the method being evaluated.}

\item{seed}{An integer seed for reproducibility.}

\item{info}{A vector of true class labels. If NULL, it will use the global 'info' variable.}
}
\value{
A data frame with ARI and NMI scores.
}
\description{
Calculate Adjusted Rand Index (ARI) and Normalized Mutual Information (NMI)
}
\details{
This function performs k-means clustering on the input data and calculates
the Adjusted Rand Index and Normalized Mutual Information between the
clustering result and a pre-existing classification.
}
\examples{
data <- matrix(rnorm(200), 100, 2)
info <- sample(1:3, 100, replace = TRUE)  # Simulated cluster information
result <- cal_ari_nmi(data, k = 3, method_name = "Example Method", seed = 123, info = info)

}
