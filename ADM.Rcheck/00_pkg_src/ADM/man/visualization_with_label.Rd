% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/Plotfunctions.R
\name{visualization_with_label}
\alias{visualization_with_label}
\title{Create a labeled scatter plot from UMAP visualization}
\usage{
visualization_with_label(umap_viz, method, info, dataset, color_list)
}
\arguments{
\item{umap_viz}{A matrix or data frame with UMAP coordinates (at least two columns).}

\item{method}{A string specifying the visualization method name (used for plot title).}

\item{info}{A vector of cluster labels for each data point.}

\item{dataset}{A string specifying the dataset name (not used in the current implementation).}

\item{color_list}{A named vector of colors for each cluster.}
}
\value{
A ggplot object representing the labeled scatter plot.
}
\description{
This function generates a scatter plot from UMAP coordinates with labeled cluster centers.
}
\details{
The function creates a scatter plot of UMAP coordinates, colors points by cluster,
calculates and labels cluster centers, and applies a custom theme. It uses ggrepel
for non-overlapping text labels.
}
\examples{
umap_coords <- matrix(rnorm(200), ncol = 2)
cluster_info <- rep(c("A", "B"), each = 50)
color_list <- c("A" = "red", "B" = "blue")
p <- visualization_with_label(umap_coords, "UMAP", cluster_info, "Example", color_list)
if (interactive()) {
  print(p)
}

}
